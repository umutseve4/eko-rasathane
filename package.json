{
  "name": "eko-rasathane",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test",
    "check": "node --check app.js && node --check core.mjs && node --check academic-catalog.mjs && node --check data/catalog.mjs && node --check data/academic-catalog.mjs",
    "verify": "npm run check && npm test"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
